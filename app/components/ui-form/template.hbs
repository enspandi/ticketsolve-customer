{{yield (hash
  label=(component 'ui-form/label')
  input=(component 'ui-form/input')
  submit=(
    component 'ui-button'
      type='submit'
      disabled=(not buffer.hasChanges)
      isLoading=submitTask.isRunning
  ))
  buffer
}}